# NOTE: This is a template to configure mod_weblogic, generated by Puppet.

<IfModule mod_rewrite>
  RewriteEngine on
  <% unless (@cond.nil? || @cond.empty?) %>
  RewriteCond <%= @cond %>
  <% end %>
  RewriteRule <%= @rule %>
</IfModule>
